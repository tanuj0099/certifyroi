import { useState } from 'react'
import { motion, AnimatePresence } from 'framer-motion'
import {
  TrendingUp, FileText, Map, LogIn, LogOut, User,
  Menu, X, Home, Info, Phone, BookOpen,
  Shield, ChevronRight, Sparkles, Mail, FileCheck,
  GraduationCap, Flame, CheckCircle, Brain, Award, Route, Building2
} from 'lucide-react'
import { AuthProvider, useAuth } from './hooks/useAuth.jsx'
import { ThemeProvider } from './hooks/useTheme.jsx'
import ThemeToggle from './components/ThemeToggle.jsx'
import ErrorBoundary from './components/ErrorBoundary.jsx'
import LandingPage from './components/LandingPage.jsx'
import ResumeAnalyzer from './components/ResumeAnalyzer.jsx'
import Hero from './components/Hero.jsx'
import Heatmap from './components/Heatmap.jsx'
import ModeSelector from './components/ModeSelector.jsx'
import CollegeVsCorporate from './components/CollegeVsCorporate.jsx'
import WaveBg from './components/WaveBg.jsx'

const T = { duration: 0.32, ease: [0.4, 0, 0.2, 1] }

const headingStyle = {
  fontFamily: 'Plus Jakarta Sans, sans-serif',
  fontWeight: '800', letterSpacing: '-0.02em',
  color: 'var(--text)', lineHeight: 1.05, marginBottom: '24px',
}

// ── Static page wrapper ───────────────────────────────────────
const PageWrapper = function({ children, maxWidth, padding }) {
  maxWidth = maxWidth || '800px'
  padding = padding || '88px 24px 60px'
  return (
    <div style={{ position: 'relative', minHeight: '100vh' }}>
      <WaveBg variant="app" />
      <div style={{ maxWidth: maxWidth, margin: '0 auto', padding: padding, position: 'relative', zIndex: 1 }}>
        {children}
      </div>
    </div>
  )
}

// ─────────────────────────────────────────────────────────────
// STATIC PAGES
// ─────────────────────────────────────────────────────────────
const AboutPage = function() {
  return (
    <PageWrapper maxWidth="800px">
      <motion.div initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} transition={T}>
        <h1 style={{ ...headingStyle, fontSize: 'clamp(2.5rem,6vw,4rem)' }}>
          WHY WE BUILT<br /><span style={{ color: 'var(--indigo)' }}>CERTIFYROI</span>
        </h1>
        <div className="glass" style={{ padding: '32px', marginBottom: '20px' }}>
          <p style={{ fontSize: '15px', color: 'var(--text-2)', lineHeight: '1.8', marginBottom: '16px', fontFamily: 'Inter, sans-serif' }}>
            CertifyROI was born from one frustration: every career site tells you what certifications exist, but nobody tells you whether it is worth your money.
          </p>
          <p style={{ fontSize: '15px', color: 'var(--text-2)', lineHeight: '1.8', fontFamily: 'Inter, sans-serif' }}>
            We built this for the anxious 26-year-old scrolling LinkedIn at 11pm, the ops manager wanting to switch into data, and the fresh graduate who just wants their first offer.
          </p>
        </div>
      </motion.div>
    </PageWrapper>
  )
}

const TermsPage = function() {
  return (
    <PageWrapper maxWidth="780px">
      <motion.div initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} transition={T}>
        <h1 style={{ ...headingStyle, fontSize: 'clamp(2rem,5vw,3.5rem)' }}>
          TERMS AND<br /><span style={{ color: 'var(--indigo)' }}>CONDITIONS</span>
        </h1>
        {[
          { title: '1. Acceptance', body: 'By using CertifyROI, you agree to these terms.' },
          { title: '2. Educational Purpose', body: 'All ROI projections are approximations for educational purposes only, not financial advice.' },
          { title: '3. AI Disclaimer', body: 'AI-powered analysis is generated by large language models. Results may be inaccurate.' },
          { title: '4. Data and Privacy', body: 'Resume text is processed in real-time and not stored. We do not sell personal data.' },
          { title: '5. Contact', body: 'Questions? Email hello@certifyroi.in' },
        ].map(function(s, i) {
          return (
            <div key={i} className="glass" style={{ padding: '20px', marginBottom: '10px' }}>
              <h3 style={{ fontSize: '14px', color: 'var(--text)', fontFamily: 'Plus Jakarta Sans, sans-serif', fontWeight: '700', marginBottom: '6px' }}>{s.title}</h3>
              <p style={{ fontSize: '13px', color: 'var(--text-3)', lineHeight: '1.7', fontFamily: 'Inter, sans-serif' }}>{s.body}</p>
            </div>
          )
        })}
      </motion.div>
    </PageWrapper>
  )
}

const PrivacyPage = function() {
  return (
    <PageWrapper maxWidth="780px">
      <motion.div initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} transition={T}>
        <h1 style={{ ...headingStyle, fontSize: 'clamp(2rem,5vw,3.5rem)' }}>
          PRIVACY<br /><span style={{ color: 'var(--indigo)' }}>POLICY</span>
        </h1>
        {[
          { title: 'What we collect', body: 'Email (if you sign in with Google) and anonymised usage data. Resume text is NOT stored.' },
          { title: 'How we use your data', body: 'Email for authentication only. No marketing emails unless you opt in.' },
          { title: 'Third-party services', body: 'Groq (AI inference), Firebase (authentication), Vercel (hosting).' },
          { title: 'Your rights', body: 'Request deletion at hello@certifyroi.in. Processed within 7 business days.' },
        ].map(function(s, i) {
          return (
            <div key={i} className="glass" style={{ padding: '20px', marginBottom: '10px' }}>
              <h3 style={{ fontSize: '14px', color: 'var(--text)', fontFamily: 'Plus Jakarta Sans, sans-serif', fontWeight: '700', marginBottom: '6px' }}>{s.title}</h3>
              <p style={{ fontSize: '13px', color: 'var(--text-3)', lineHeight: '1.7', fontFamily: 'Inter, sans-serif' }}>{s.body}</p>
            </div>
          )
        })}
      </motion.div>
    </PageWrapper>
  )
}

const BLOG_POSTS = [
  { id: 1, tag: 'Cloud', tagColor: '#6366F1', title: 'Is AWS Solutions Architect Worth It in 2026?', excerpt: 'With 2,400+ open roles on Naukri and average hikes of 30-40%, AWS SAA remains the highest-ROI cert for Indian engineers.', readTime: '4 min', date: 'Mar 2026', emoji: 'cloud', forWho: 'Professional' },
  { id: 2, tag: 'Career Switch', tagColor: '#F59E0B', title: 'From Ops Manager to Data Analyst: A 6-Month Plan', excerpt: 'Three real people. Three backgrounds. One goal: break into data from operations.', readTime: '6 min', date: 'Feb 2026', emoji: 'switch', forWho: 'Switcher' },
  { id: 3, tag: 'Student', tagColor: '#818CF8', title: "Fresher Guide to First Offer in India", excerpt: 'No experience. No salary history. But you can get a strong first offer in 5-8 months.', readTime: '8 min', date: 'Feb 2026', emoji: 'grad', forWho: 'Student' },
]

const BlogPage = function() {
  const [filter, setFilter] = useState('All')
  const filtered = filter === 'All' ? BLOG_POSTS : BLOG_POSTS.filter(function(p) { return p.forWho === filter })
  return (
    <PageWrapper maxWidth="1000px">
      <motion.div initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} transition={T}>
        <h1 style={{ ...headingStyle, fontSize: 'clamp(2.5rem,6vw,4rem)' }}>
          THE CERTIFYROI<br /><span style={{ color: 'var(--indigo)' }}>BLOG</span>
        </h1>
        <div style={{ display: 'flex', gap: '8px', marginBottom: '28px', flexWrap: 'wrap' }}>
          {['All', 'Student', 'Switcher', 'Professional'].map(function(t) {
            return (
              <button key={t} onClick={function() { setFilter(t) }}
                style={{ padding: '6px 16px', borderRadius: '20px', fontSize: '13px', cursor: 'pointer', fontFamily: 'Inter, sans-serif', fontWeight: '600', transition: 'all 0.18s', background: filter === t ? 'var(--indigo-dim)' : 'var(--surface)', border: '1px solid ' + (filter === t ? 'var(--border-accent)' : 'var(--border)'), color: filter === t ? 'var(--indigo-light)' : 'var(--text-4)' }}>
                {t}
              </button>
            )
          })}
        </div>
        <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fill,minmax(290px,1fr))', gap: '16px' }}>
          {filtered.map(function(post, i) {
            return (
              <motion.div key={post.id} initial={{ opacity: 0, y: 14 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: i * 0.07 }} className="glass"
                style={{ padding: '22px', cursor: 'pointer' }} whileHover={{ y: -3 }}
                onClick={function() { alert(post.title + '\n\nFull post coming soon!') }}>
                <div style={{ fontSize: '10px', fontWeight: '700', padding: '3px 8px', borderRadius: '6px', background: post.tagColor + '18', color: post.tagColor, display: 'inline-block', marginBottom: '10px', fontFamily: 'JetBrains Mono, monospace' }}>{post.tag}</div>
                <h3 style={{ fontSize: '14px', fontWeight: '700', color: 'var(--text)', marginBottom: '8px', lineHeight: '1.4', fontFamily: 'Plus Jakarta Sans, sans-serif' }}>{post.title}</h3>
                <p style={{ fontSize: '12px', color: 'var(--text-3)', lineHeight: '1.6', fontFamily: 'Inter, sans-serif' }}>{post.excerpt}</p>
              </motion.div>
            )
          })}
        </div>
      </motion.div>
    </PageWrapper>
  )
}

const FAQPage = function() {
  const [open, setOpen] = useState(null)
  const faqs = [
    { q: 'Is CertifyROI free?', a: 'Yes. ROI calculator, heatmap, and city data are free. You get 3 free AI analyses as a guest. Sign in with Google for unlimited.' },
    { q: 'How accurate are the salary figures?', a: 'Sourced from LinkedIn Economic Graph, NASSCOM 2025, Naukri salary reports, and AmbitionBox. Figures are medians.' },
    { q: 'Is my resume stored anywhere?', a: 'No. Resume text is processed in real-time and immediately discarded. Never stored, logged, or shared.' },
    { q: 'Why does Student Mode exist?', a: 'ROI calculators assume you have a salary. Students do not. Student Mode reframes around time to first offer.' },
    { q: 'Is this only for tech careers?', a: 'No. Finance (CFA, FMVA), Marketing, HR, Product, Design, Law, Medical, Architecture, and Government exams are all covered.' },
  ]
  return (
    <PageWrapper maxWidth="740px">
      <motion.div initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} transition={T}>
        <h1 style={{ ...headingStyle, fontSize: 'clamp(2.5rem,6vw,4rem)' }}>
          FREQUENTLY<br /><span style={{ color: 'var(--indigo)' }}>ASKED</span>
        </h1>
        <div style={{ display: 'flex', flexDirection: 'column', gap: '8px' }}>
          {faqs.map(function(faq, i) {
            return (
              <motion.div key={i} className="glass" initial={{ opacity: 0, y: 8 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: i * 0.04 }}
                style={{ overflow: 'hidden', cursor: 'pointer' }} onClick={function() { setOpen(open === i ? null : i) }}>
                <div style={{ padding: '16px 20px', display: 'flex', alignItems: 'center', justifyContent: 'space-between', gap: '12px' }}>
                  <span style={{ fontSize: '14px', fontWeight: '600', color: open === i ? 'var(--indigo-light)' : 'var(--text)', fontFamily: 'Plus Jakarta Sans, sans-serif' }}>{faq.q}</span>
                  <ChevronRight size={15} color="var(--text-4)" style={{ flexShrink: 0, transform: open === i ? 'rotate(90deg)' : 'none', transition: 'transform 0.2s' }} />
                </div>
                <AnimatePresence>
                  {open === i ? (
                    <motion.div initial={{ height: 0, opacity: 0 }} animate={{ height: 'auto', opacity: 1 }} exit={{ height: 0, opacity: 0 }} transition={{ duration: 0.22 }} style={{ overflow: 'hidden' }}>
                      <div style={{ padding: '0 20px 16px', paddingTop: '12px', fontSize: '14px', color: 'var(--text-2)', borderTop: '1px solid var(--border)', fontFamily: 'Inter, sans-serif', lineHeight: '1.7' }}>{faq.a}</div>
                    </motion.div>
                  ) : null}
                </AnimatePresence>
              </motion.div>
            )
          })}
        </div>
      </motion.div>
    </PageWrapper>
  )
}

const ContactPage = function() {
  const [sent, setSent] = useState(false)
  const [formData, setFormData] = useState({ name: '', email: '', message: '' })
  const inputStyle = { width: '100%', padding: '11px 14px', background: 'var(--surface)', border: '1px solid var(--border)', borderRadius: '10px', color: 'var(--text)', fontSize: '14px', fontFamily: 'Inter, sans-serif', outline: 'none' }
  return (
    <PageWrapper maxWidth="600px">
      <motion.div initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} transition={T}>
        <h1 style={{ ...headingStyle, fontSize: 'clamp(2.5rem,6vw,4rem)' }}>CONTACT<br /><span style={{ color: 'var(--indigo)' }}>US</span></h1>
        {sent ? (
          <div className="glass" style={{ padding: '40px', textAlign: 'center' }}>
            <div style={{ fontSize: '14px', color: 'var(--text)', fontFamily: 'Plus Jakarta Sans, sans-serif' }}>Message sent! We will reply within 48 hours.</div>
          </div>
        ) : (
          <div className="glass" style={{ padding: '28px' }}>
            <div style={{ marginBottom: '14px' }}>
              <input type="text" placeholder="Your name" value={formData.name} onChange={function(e) { setFormData(function(p) { return { ...p, name: e.target.value } }) }} style={inputStyle} />
            </div>
            <div style={{ marginBottom: '14px' }}>
              <input type="email" placeholder="you@email.com" value={formData.email} onChange={function(e) { setFormData(function(p) { return { ...p, email: e.target.value } }) }} style={inputStyle} />
            </div>
            <div style={{ marginBottom: '20px' }}>
              <textarea rows={4} placeholder="Your message..." value={formData.message} onChange={function(e) { setFormData(function(p) { return { ...p, message: e.target.value } }) }} style={{ ...inputStyle, resize: 'vertical' }} />
            </div>
            <button className="btn-primary" style={{ width: '100%' }} onClick={function() { setSent(true) }}>Send Message</button>
          </div>
        )}
      </motion.div>
    </PageWrapper>
  )
}

// ─────────────────────────────────────────────────────────────
// NAV CONFIG
// ─────────────────────────────────────────────────────────────
const NAV_LINKS = [
  { id: 'home',    label: 'Home',    icon: Home       },
  { id: 'app',     label: 'Tools',   icon: TrendingUp },
  { id: 'blog',    label: 'Blog',    icon: BookOpen   },
  { id: 'faq',     label: 'FAQ',     icon: Sparkles   },
  { id: 'about',   label: 'About',   icon: Info       },
  { id: 'contact', label: 'Contact', icon: Phone      },
]

const APP_TABS = [
  { id: 'resume',     label: 'Step 1 - Find Cert',  icon: FileText,      desc: 'Upload resume, AI picks your cert'    },
  { id: 'calculator', label: 'Step 2 - ROI',         icon: TrendingUp,    desc: 'Break-even, 5yr gain'                 },
  { id: 'heatmap',    label: 'Step 3 - City',        icon: Map,           desc: 'Is this cert hot in your city?'       },
  { id: 'compare',    label: 'Step 4 - Compare',     icon: Award,         desc: 'Compare two certs side by side'       },
  { id: 'simulate',   label: 'Step 5 - Simulate',    icon: Route,         desc: 'Career path and salary trajectory'    },
  { id: 'jobmap',     label: 'Cert to Job Map',      icon: Building2,     desc: 'Which cert gets which role'           },
  { id: 'college',    label: 'Degree vs Certs',      icon: GraduationCap, desc: 'MBA vs 4 certifications'              },
]

// ─────────────────────────────────────────────────────────────
// NAVBAR
// ─────────────────────────────────────────────────────────────
const NavBar = function({ currentPage, activeTab, onNavigate, onTabChange }) {
  const [menuOpen, setMenuOpen] = useState(false)
  const [signingIn, setSigningIn] = useState(false)
  const { user, signInGoogle, signOut, loading } = useAuth()

  const go = function(id) { onNavigate(id); setMenuOpen(false) }

  const handleSignIn = async function() {
    setSigningIn(true)
    try { await signInGoogle() } catch(e) {}
    setSigningIn(false)
  }

  const navH  = 64
  const tabsH = 48

  return (
    <>
      <motion.header initial={{ y: -20, opacity: 0 }} animate={{ y: 0, opacity: 1 }} transition={T}
        style={{ position: 'fixed', top: 0, left: 0, right: 0, zIndex: 100, background: 'var(--bg)', backdropFilter: 'blur(20px)', WebkitBackdropFilter: 'blur(20px)', borderBottom: '1px solid var(--border)' }}>
        <div style={{ maxWidth: '1240px', margin: '0 auto', padding: '0 16px' }}>
          <div style={{ display: 'flex', alignItems: 'center', height: navH + 'px', gap: '4px' }}>
            <button onClick={function() { go('home') }}
              style={{ background: 'none', border: 'none', cursor: 'pointer', display: 'flex', alignItems: 'center', gap: '8px', flexShrink: 0, padding: '0 8px 0 0', marginRight: '8px' }}>
              <div style={{ width: '30px', height: '30px', background: 'linear-gradient(135deg, var(--indigo), #4338CA)', borderRadius: '8px', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
                <TrendingUp size={15} color="white" />
              </div>
              <span style={{ fontFamily: 'Plus Jakarta Sans, sans-serif', fontWeight: '800', fontSize: '17px', letterSpacing: '-0.02em', color: 'var(--text)' }}>
                Certify<span style={{ color: 'var(--indigo)' }}>ROI</span>
              </span>
            </button>

            <nav style={{ display: 'flex', gap: '2px', flex: 1, justifyContent: 'center', alignItems: 'center' }}>
              {NAV_LINKS.map(function(link) {
                const isActive = currentPage === link.id
                return (
                  <button key={link.id} onClick={function() { go(link.id) }}
                    style={{ display: 'flex', alignItems: 'center', gap: '5px', padding: '7px 14px', borderRadius: '8px', border: '1px solid ' + (isActive ? 'var(--border-accent)' : 'transparent'), background: isActive ? 'var(--indigo-dim)' : 'transparent', color: isActive ? 'var(--indigo-light)' : 'var(--text-4)', fontSize: '13px', fontWeight: isActive ? '700' : '500', cursor: 'pointer', fontFamily: 'Plus Jakarta Sans, sans-serif', whiteSpace: 'nowrap', transition: 'all 0.18s' }}>
                    <link.icon size={12} />{link.label}
                  </button>
                )
              })}
            </nav>

            <div style={{ display: 'flex', alignItems: 'center', gap: '6px', flexShrink: 0, marginLeft: 'auto' }}>
              {!loading ? (
                user ? (
                  <>
                    <div style={{ display: 'flex', alignItems: 'center', gap: '6px' }}>
                      {user.photoURL ? (
                        <img src={user.photoURL} alt="" style={{ width: '28px', height: '28px', borderRadius: '50%', border: '2px solid var(--border-accent)' }} />
                      ) : (
                        <div style={{ width: '28px', height: '28px', borderRadius: '50%', background: 'linear-gradient(135deg, var(--indigo), #10B981)', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
                          <User size={13} color="white" />
                        </div>
                      )}
                    </div>
                    <button onClick={signOut} style={{ background: 'rgba(239,68,68,0.1)', border: '1px solid rgba(239,68,68,0.2)', borderRadius: '7px', color: '#EF4444', fontSize: '12px', padding: '5px 10px', cursor: 'pointer', display: 'flex', alignItems: 'center', gap: '4px', fontFamily: 'Inter, sans-serif' }}>
                      <LogOut size={12} /> Sign out
                    </button>
                  </>
                ) : (
                  <button onClick={handleSignIn} disabled={signingIn}
                    style={{ display: 'flex', alignItems: 'center', gap: '5px', background: 'var(--indigo)', border: 'none', borderRadius: '8px', padding: '7px 14px', cursor: 'pointer', color: 'white', fontSize: '13px', fontFamily: 'Inter, sans-serif', fontWeight: '600' }}>
                    <LogIn size={13} />{signingIn ? '...' : 'Sign in'}
                  </button>
                )
              ) : null}
              <ThemeToggle />
              <button onClick={function() { setMenuOpen(function(v) { return !v }) }}
                style={{ background: 'var(--surface)', border: '1px solid var(--border)', borderRadius: '7px', padding: '7px', cursor: 'pointer', color: 'var(--text-3)', display: 'flex', alignItems: 'center' }}>
                {menuOpen ? <X size={15} /> : <Menu size={15} />}
              </button>
            </div>
          </div>
        </div>

        {currentPage === 'app' ? (
          <div style={{ borderTop: '1px solid var(--border)', background: 'var(--bg)' }}>
            <div style={{ maxWidth: '1240px', margin: '0 auto', padding: '0 16px', display: 'flex', height: tabsH + 'px', alignItems: 'stretch', overflowX: 'auto', gap: '2px', justifyContent: 'center' }}>
              {APP_TABS.map(function(tab) {
                const active = activeTab === tab.id
                return (
                  <button key={tab.id} onClick={function() { onTabChange(tab.id) }}
                    style={{ display: 'flex', alignItems: 'center', gap: '6px', padding: '0 14px', background: active ? 'var(--indigo-dim)' : 'transparent', border: 'none', borderBottom: '2px solid ' + (active ? 'var(--indigo)' : 'transparent'), color: active ? 'var(--indigo-light)' : 'var(--text-3)', fontSize: '12px', fontWeight: active ? '700' : '500', cursor: 'pointer', fontFamily: 'Plus Jakarta Sans, sans-serif', transition: 'all 0.2s', whiteSpace: 'nowrap', flexShrink: 0 }}>
                    <tab.icon size={12} />{tab.label}
                  </button>
                )
              })}
            </div>
          </div>
        ) : null}
      </motion.header>

      <AnimatePresence>
        {menuOpen ? (
          <motion.div initial={{ opacity: 0, y: -8 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: -8 }} transition={{ duration: 0.2 }}
            style={{ position: 'fixed', top: currentPage === 'app' ? (navH + tabsH) + 'px' : navH + 'px', left: 0, right: 0, zIndex: 99, background: 'var(--bg)', backdropFilter: 'blur(20px)', borderBottom: '1px solid var(--border)', padding: '20px 16px' }}>
            <div style={{ maxWidth: '1240px', margin: '0 auto' }}>
              <div style={{ fontSize: '10px', color: 'var(--text-4)', textTransform: 'uppercase', letterSpacing: '0.1em', marginBottom: '10px', fontFamily: 'JetBrains Mono, monospace' }}>Tools</div>
              <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fill,minmax(160px,1fr))', gap: '8px', marginBottom: '20px' }}>
                {APP_TABS.map(function(tab) {
                  return (
                    <button key={tab.id} onClick={function() { go('app'); onTabChange(tab.id) }}
                      style={{ padding: '14px 12px', borderRadius: '10px', cursor: 'pointer', textAlign: 'left', background: 'var(--surface)', border: '1px solid var(--border)', transition: 'all 0.18s' }}>
                      <tab.icon size={16} color="var(--indigo)" style={{ marginBottom: '6px', display: 'block' }} />
                      <div style={{ fontSize: '12px', fontWeight: '700', color: 'var(--text)', marginBottom: '2px', fontFamily: 'Plus Jakarta Sans, sans-serif' }}>{tab.label}</div>
                      <div style={{ fontSize: '11px', color: 'var(--text-4)', fontFamily: 'Inter, sans-serif' }}>{tab.desc}</div>
                    </button>
                  )
                })}
              </div>
              <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fill,minmax(120px,1fr))', gap: '6px' }}>
                {[
                  { id: 'blog',    label: 'Blog',    icon: BookOpen  },
                  { id: 'faq',     label: 'FAQ',     icon: Sparkles  },
                  { id: 'about',   label: 'About',   icon: Info      },
                  { id: 'contact', label: 'Contact', icon: Phone     },
                  { id: 'terms',   label: 'Terms',   icon: FileCheck },
                  { id: 'privacy', label: 'Privacy', icon: Shield    },
                ].map(function(item) {
                  return (
                    <button key={item.id} onClick={function() { go(item.id) }}
                      style={{ padding: '10px 12px', borderRadius: '8px', background: 'var(--surface)', border: '1px solid var(--border)', cursor: 'pointer', display: 'flex', alignItems: 'center', gap: '7px', color: 'var(--text-2)', fontSize: '12px', fontFamily: 'Inter, sans-serif' }}>
                      <item.icon size={12} color="var(--text-4)" />{item.label}
                    </button>
                  )
                })}
              </div>
            </div>
          </motion.div>
        ) : null}
      </AnimatePresence>
    </>
  )
}

// ─────────────────────────────────────────────────────────────
// APP PAGE
// ─────────────────────────────────────────────────────────────
const AppPage = function({ activeTab, onTabChange, mode, onModeChange, onCertSelected, prefilledCert, resumeCity, resumeDomain, resumeName }) {
  const NAV_H  = 64
  const TABS_H = 48

  return (
    <div style={{ paddingTop: (NAV_H + TABS_H) + 'px', minHeight: '100vh', background: 'var(--app-bg)', position: 'relative' }}>
      <WaveBg variant="app" />
      <div style={{ position: 'relative', zIndex: 1 }}>
        <div style={{ maxWidth: '1100px', margin: '0 auto', padding: '20px 20px 0' }}>
          <ModeSelector activeMode={mode} onChange={onModeChange} />
        </div>

        <AnimatePresence mode="wait">
          <motion.div key={activeTab} initial={{ opacity: 0, y: 10 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: -6 }} transition={T}>
            <div style={{ maxWidth: '1100px', margin: '0 auto', padding: '0 20px 60px' }}>

              {activeTab === 'resume' ? (
                <div className="glass" style={{ padding: '28px' }}>
                  <ResumeAnalyzer mode={mode} onCertSelected={function(certName, city, domain, name) { onCertSelected(certName, city, domain, name) }} />
                </div>
              ) : null}

              {activeTab === 'calculator' ? (
                <Hero mode={mode} prefilledCert={prefilledCert} resumeName={resumeName} resumeCity={resumeCity} />
              ) : null}

              {activeTab === 'heatmap' ? (
                <div className="glass" style={{ padding: '28px' }}>
                  <Heatmap prefilledCity={resumeCity} prefilledDomain={resumeDomain} certName={prefilledCert} resumeName={resumeName} />
                </div>
              ) : null}

              {activeTab === 'college' ? (
                <div className="glass" style={{ padding: '28px' }}>
                  <CollegeVsCorporate />
                </div>
              ) : null}

              {activeTab === 'compare' ? (
                <div className="glass" style={{ padding: '28px' }}>
                  <div style={{ textAlign: 'center', padding: '40px', color: 'var(--text-3)', fontFamily: 'Inter, sans-serif' }}>
                    CertCompare — coming soon. Deploy CertCompare.jsx to activate.
                  </div>
                </div>
              ) : null}

              {activeTab === 'simulate' ? (
                <div className="glass" style={{ padding: '28px' }}>
                  <div style={{ textAlign: 'center', padding: '40px', color: 'var(--text-3)', fontFamily: 'Inter, sans-serif' }}>
                    CareerSimulator — coming soon. Deploy CareerSimulator.jsx to activate.
                  </div>
                </div>
              ) : null}

              {activeTab === 'jobmap' ? (
                <div className="glass" style={{ padding: '28px' }}>
                  <div style={{ textAlign: 'center', padding: '40px', color: 'var(--text-3)', fontFamily: 'Inter, sans-serif' }}>
                    JobCertMap — coming soon. Deploy JobCertMap.jsx to activate.
                  </div>
                </div>
              ) : null}

            </div>
          </motion.div>
        </AnimatePresence>
      </div>
    </div>
  )
}

// ─────────────────────────────────────────────────────────────
// FOOTER
// ─────────────────────────────────────────────────────────────
const Footer = function({ onNavigate }) {
  return (
    <footer style={{ borderTop: '1px solid var(--border)', padding: '36px 24px 20px', marginTop: 'auto' }}>
      <div style={{ maxWidth: '1240px', margin: '0 auto' }}>
        <div style={{ display: 'flex', alignItems: 'center', gap: '7px', marginBottom: '16px' }}>
          <div style={{ width: '26px', height: '26px', background: 'linear-gradient(135deg, var(--indigo), #4338CA)', borderRadius: '7px', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
            <TrendingUp size={13} color="white" />
          </div>
          <span style={{ fontFamily: 'Plus Jakarta Sans, sans-serif', fontWeight: '800', fontSize: '16px', color: 'var(--text)' }}>
            Certify<span style={{ color: 'var(--indigo)' }}>ROI</span>
          </span>
        </div>
        <p style={{ fontSize: '12px', color: 'var(--text-4)', fontFamily: 'Inter, sans-serif' }}>
          2026 CertifyROI. Data-driven cert decisions for India's tech professionals.
        </p>
        <div style={{ display: 'flex', gap: '16px', marginTop: '12px', flexWrap: 'wrap' }}>
          {[
            { id: 'terms',   label: 'Terms'   },
            { id: 'privacy', label: 'Privacy' },
            { id: 'about',   label: 'About'   },
            { id: 'contact', label: 'Contact' },
          ].map(function(l) {
            return (
              <button key={l.label} onClick={function() { onNavigate(l.id) }}
                style={{ background: 'none', border: 'none', color: 'var(--text-4)', fontSize: '12px', cursor: 'pointer', fontFamily: 'Inter, sans-serif', padding: 0 }}>
                {l.label}
              </button>
            )
          })}
        </div>
      </div>
    </footer>
  )
}

// ─────────────────────────────────────────────────────────────
// ROOT APP
// ─────────────────────────────────────────────────────────────
function AppRoot() {
  const [page,          setPage]          = useState('home')
  const [activeTab,     setActiveTab]     = useState('resume')
  const [mode,          setMode]          = useState('professional')
  const [prefilledCert, setPrefilledCert] = useState('')
  const [resumeCity,    setResumeCity]    = useState('')
  const [resumeDomain,  setResumeDomain]  = useState('')
  const [resumeName,    setResumeName]    = useState('')

  const navigate = function(pageId) {
    setPage(pageId)
    window.scrollTo({ top: 0, behavior: 'smooth' })
  }

  const goToApp = function(tab) {
    setActiveTab(tab || 'resume')
    navigate('app')
  }

  const handleCertSelected = function(certName, city, domain, name) {
    setPrefilledCert(certName)
    if (city)   setResumeCity(city)
    if (domain) setResumeDomain(domain)
    if (name)   setResumeName(name)
    setActiveTab('calculator')
  }

  const renderPage = function() {
    if (page === 'home')    return <LandingPage onEnter={function() { goToApp('resume') }} />
    if (page === 'app')     return <AppPage activeTab={activeTab} onTabChange={setActiveTab} mode={mode} onModeChange={setMode} onCertSelected={handleCertSelected} prefilledCert={prefilledCert} resumeCity={resumeCity} resumeDomain={resumeDomain} resumeName={resumeName} />
    if (page === 'blog')    return <BlogPage />
    if (page === 'faq')     return <FAQPage />
    if (page === 'about')   return <AboutPage />
    if (page === 'contact') return <ContactPage />
    if (page === 'terms')   return <TermsPage />
    if (page === 'privacy') return <PrivacyPage />
    return <LandingPage onEnter={function() { goToApp('resume') }} />
  }

  return (
    <div style={{ minHeight: '100vh', display: 'flex', flexDirection: 'column' }}>
      <NavBar currentPage={page} activeTab={activeTab} onNavigate={navigate} onTabChange={setActiveTab} />
      <main style={{ flex: 1 }}>
        <AnimatePresence mode="wait">
          <motion.div key={page + activeTab} initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }} transition={T}>
            {renderPage()}
          </motion.div>
        </AnimatePresence>
      </main>
      <Footer onNavigate={navigate} />
    </div>
  )
}

export default function App() {
  return (
    <ErrorBoundary>
      <ThemeProvider>
        <AuthProvider>
          <AppRoot />
        </AuthProvider>
      </ThemeProvider>
    </ErrorBoundary>
  )
}